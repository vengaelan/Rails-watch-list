<h1>Save any kind of movies</h1>

<%= link_to 'Create a movie list', new_list_path, class: 'btn btn-flat' %>

<% @lists.each do |list| %>
<p><%= list.name %><%= link_to list_path(list) %></p>
<% end %>
